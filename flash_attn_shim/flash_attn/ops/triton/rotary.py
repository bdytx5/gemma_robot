# Stub – flash_attn.ops.triton.rotary (SDPA shim for sm_120)

def apply_rotary(*args, **kwargs):
    """Stub — should never be called when using SDPA."""
    raise RuntimeError("flash_attn.ops.triton.rotary.apply_rotary stub called")
